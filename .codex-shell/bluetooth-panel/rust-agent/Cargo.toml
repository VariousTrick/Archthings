[package]
name = "codex-bt-agentd"
version = "0.1.0"
edition = "2021"

[dependencies]
async-channel = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
zbus = { version = "4.4", default-features = false, features = ["tokio"] }
